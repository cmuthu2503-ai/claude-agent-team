; Minimal pytest config for the agent-team-mcp service (HAI-07). Its own test
; harness — the MCP service is outside the backend's --cov=src gate. HAI-53
; expands this with a coverage config + the MCP↔backend contract test.
[pytest]
asyncio_mode = auto
testpaths = tests
