# agent-team-mcp dependencies (HAI-05)
# Official MCP Python SDK — provides FastMCP + the streamable-HTTP transport.
mcp>=1.2.0
# HTTP client for the REST adapter (HAI-07) that calls the Agent Team backend.
httpx>=0.27
